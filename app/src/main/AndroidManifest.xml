<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hirasawarei.sns_beta">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/orizuru2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/orizuru2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".account.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".account.LoginActivity" />
        <activity android:name=".myPage.MyPageActivity" />
        <activity android:name=".uploadPosts.AddPhotoActivity" />
        <activity android:name=".timeLine.TimeLineActivity" />
        <activity android:name=".search.SearchActivity" />
        <activity android:name=".FriendsActivity" />
        <activity android:name=".friends.FriendsActivity" />
        <activity android:name=".ProfileActivity"></activity>
    </application>

</manifest>